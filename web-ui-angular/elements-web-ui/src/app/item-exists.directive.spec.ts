import { ItemExistsDirective } from './item-exists.directive';

describe('ItemExistsDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemExistsDirective();
    expect(directive).toBeTruthy();
  });
});
