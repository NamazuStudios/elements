package dev.getelements.elements.rt.annotation;

import java.lang.annotation.Retention;

/**
 * Used to explicitly document a class as a binding to a scripting language. In order to generate document a particular
 * element, then this must annotation must appear in the hierarchy.
 */
public @interface Public {}
