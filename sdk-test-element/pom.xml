<?xml version="1.0"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>dev.getelements.elements</groupId>
        <artifactId>eci-elements</artifactId>
        <version>3.6.7-SNAPSHOT</version>
    </parent>

    <artifactId>sdk-test-element</artifactId>
    <name>${project.artifactId}</name>
    <version>3.6.7-SNAPSHOT</version>
    <url>https://namazustudios.com</url>

    <properties>
        <!-- This is the cleanest way to signal this module doesn't produce Javadoc -->
        <maven.javadoc.skip>true</maven.javadoc.skip>
    </properties>

    <dependencies>
        <dependency>
            <groupId>dev.getelements.elements</groupId>
            <artifactId>sdk</artifactId>
        </dependency>
        <dependency>
            <groupId>dev.getelements.elements</groupId>
            <artifactId>sdk-test-api</artifactId>
        </dependency>
    </dependencies>

</project>
