/**
 * Root package of the cluster fabric SDK: the identifiers, {@code Path} addressing scheme, and remote invocation
 * machinery used to locate and invoke elements hosted across instances in the cluster.
 */
@ElementPublic
package dev.getelements.elements.sdk.cluster;

import dev.getelements.elements.sdk.annotation.ElementPublic;
