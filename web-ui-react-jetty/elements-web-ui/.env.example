# Backend URL (development)
ELEMENTS_BACKEND_URL=http://localhost:8080

# Frontend base URL for production builds
# This should match where the admin interface is mounted (e.g., /admin)
VITE_ELEMENTS_BACKEND_URL=http://localhost:8080

# Test credentials for integration tests
TEST_SUPERUSER_USERNAME=root
TEST_SUPERUSER_PASSWORD=password
